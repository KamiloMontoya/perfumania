
<hr>
<h1>Listado de Notas olfativas
<button type="button" class="btn btn-success" data-toggle="modal" data-target="#newModal">Nueva Nota olfativa </button>
</h1>

<hr>

<table class="table table-hover table-bordered table-responsive" text-align="center">
  <thead>
    <tr>
      <th class="text-center">Nombre</th>
      <th class="text-center" colspan="3">Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.name %></td>
        <td><%= link_to 'Editar', edit_backend_note_path(note), {:class=>"btn btn-warning bt-sm" } %></td>
        <td><%= link_to 'Eliminar', [:backend, note], {method: :delete, data: { confirm: 'Desea eliminar esta nota olfativa?' }, :class=>"btn btn-danger bt-sm" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @notes, :theme => 'twitter-bootstrap-3', :pagination_class => "pagination-small pagination-centered" %>


<!-- Modal para nueva Nota -->
<div class="modal fade" id="newModal" role="dialog">
  <div class="modal-dialog">
    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Nueva nota olfativa:</h4>
      </div>
      <div class="modal-body"> 
       <%= form_for([:backend, Note.new],{:class => "form-horizontal" }) do |f| %>
          <div class="form-group">
            <label class="control-label">Nombre: </label>
            <%= f.text_field :name, {:class => "form-control"} %>
          </div> 
          <div class="form-group">
              <%= f.submit 'Agregar', :class => "btn btn-success"%>
          </div>       
        <% end %>        
      </div>
    </div>
  </div>
</div>