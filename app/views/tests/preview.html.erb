
<% number = 1 %>
<div class="row">
	<h2> <%= @test.name%> </h2>
	<p> <%= @test.description %> </p>		
</div>

<%= form_tag('/preview_proccess', :method => "post") do %>
	<div class="form-group" style="display:none">
		<input name="test_id" value=<%= @test.id%> > </input>
	</div>
	<div class="form-group">
	    <%= label_tag "name", "Nombres y Apellidos" %>
	    <%= text_field_tag "name", "", :required => "required" %>
    </div>
	<div class="form-group">
		<%= label_tag "email", "Correo Electrónico" %>
	    <%= text_field_tag "email", "", :required => "required" %>
    </div>
	<% @test.questions.each do |question| %>
		<% leter= 'A' %>
	 	<div class="form-group radio_buttons_inline required visitor_favourite">
	    <label class="radio-inline"><%= number.to_s %> <%= '. ' << question.title %></label>
			<% question.answers.each do |answer| %>
			<input type="radio" name=<%= 'question-'+question.id.to_s %> id=<%= 'question-'+question.id.to_s%> value=<%= answer.id%> >
			<%= leter.chr %> 
				<%='. ' << answer.title %>
			<% leter = leter.ord + 1 %>
			<% end %>
			</input>
		</div>
		<% number += 1 %>
	<% end %>

	 <%= submit_tag "Enviar", :id => "submit", :name => "submit", :class => "form_submit", :disabled => false, :disable_with => "Por favor espere..." %>
<% end %> <!-- End Form -->